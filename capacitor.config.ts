import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.arataex5.twixt",
  appName: "TWIXT",
  webDir: "dist",
  android: { allowMixedContent: false },
};

export default config;
